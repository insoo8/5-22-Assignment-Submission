6.마라탕후루
#include <stdio.h>
int main() {
    int N,P,Q;
     scanf("%d %d %d",&N,&P,&Q); // N,P,Q값 입력
    int a[N],b[N],c[N]; 
    for(int i=0;i<N;i++){
        scanf("%d",&a[i]); // 배열 a값 입력
    }
    for(int j=0;j<N;j++){
        scanf("%d",&b[j]); // 배열 b 값 입력
    }
    for(int i=0;i<N;i++){
        for(int j=0;j<=10000;j++){
            if(a[i]==b[i]){
                c[i]=j; // 만약 a[i]의 값과 b[i]의 값이 같다면 c[i]에 반복된횟수 j값 넣기
                break;
            }
            a[i]+=P; // a[i]값을 P만큼 증가
            b[i]+=Q; // b[i[값을 Q만큼 증가
            if(j==10000){
                c[i]=-1; // 만약 실행횟수 j가 10000회가된다면 c[i]에 -1넣기
                break;
            }
        }
    }
    int x=0;
     for(int i=0;i<N;i++){ // N번반복하기
         if(c[i]==-1){ 
             printf("NO\n"); // 만약 c[i]에 -1값이 들어있으면 NO출력
             x=1; // x에 1넣기
             break;
         }
     }
    if(x==0){
        printf("YES\n"); // 만약 x가 0이라면 YES출력
        for(int i=0;i<N;i++){
            printf("%d ",c[i]); c배열 값 반복문으로 모두 출력하기
        }
    }
    return 0;
}
꼬치개수 N만큼 딸기배열과 샤인머스켓배열을 입력받고 딸기배열과 샤인머스켓배열에 각각 P,Q씩 딸기배열과 샤인머스켓배열의 같은 순서값끼리 같을때 까지 더하는데 만약 같아지면 실행횟수 j를 실행횟수를 저장해놓는 배열 c에 저장한다.
만약 실행횟수가 10000회가 된다면 c배열에 -1값을 저장한다. 마지막으로 c배열에 저장된 값들과 -1을 비교하며 만약 같다면 10000회가 될때까지 같아지지 않은것이므로 NO를 출력하며 프로그램이 종료되고 아니라면 YES를 출력하고
C배열속에 저장된 값을 모두 출력한다.
