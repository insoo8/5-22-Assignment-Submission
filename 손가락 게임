2.손가락 게임
#include <stdio.h>
int main() {
    int a,b;
    scanf("%d %d",&a ,&b); // a,b입력
    if((a==2&&b==2)||(a==5&&b==5)||(a==0&&b==0))
        printf("="); // 만약 a와 b가 0,2,5에서 같은 값이 입력되면 = 출력
    if((a!=2&&a!=5&&a!=0)&&(b!=2&&b!=5&&b!=0))
       printf("="); // 만약 a와 b둘다 0,2,5중 어느 값도 가지지 않는다면 = 출력
      else if((a==2||a==5||a==0)&&(b!=2&&b!=5&&b!=0))
        printf(">"); // a가 0,2,5중 값을 가지고있고 b는 어느값도 가지지 않을때 > 출력 
      else if((b==2||b==5||b==0)&&(a!=2&&a!=5&&a!=0))
        printf("<"); // 반대의 경우에 < 출력
    if(a==0&&b==2)
        printf(">"); // a가 0이고 b가 2일때 > 출력
    if(a==2&&b==5)
        printf(">"); // a가 2이고 b가 5일때 > 출력
    if(a==5&&b==0) 
        printf(">");  // a가 5이고 b가 0일때 > 출력
    if(a==0&&b==5)
        printf("<"); // a가 0이고 b가 5일때 < 출력
    if(a==2&&b==0)
        printf("<"); // a가 2이고 b가 0일때 < 출력
    if(a==5&&b==2)
        printf("<"); // a가 5이고 b가 2일때 < 출력
   
    return 0;
}
간단한 가위바위보규칙으로 코드 짰습니다.
